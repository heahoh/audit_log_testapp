{% extends 'base.html.twig' %}

{% block title %}Seller{% endblock %}

{% block body %}
    <h1>Seller</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seller.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seller.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_seller_index') }}">back to list</a>

    <a href="{{ path('app_seller_edit', {'id': seller.id}) }}">edit</a>

    {{ include('seller/_delete_form.html.twig') }}
{% endblock %}
